# Development ledger note — June 2026 sprint

**Project:** OnPro (Next.js workspace + Supabase backend path)  
**Sprint:** 1 June – 30 June 2026 (full month)  
**Purpose:** Running task list for updating [`src/lib/ledger/seed.ts`](../../src/lib/ledger/seed.ts) → `workRecords`, cap notes, and June retainer accounting.

Use checkboxes as work lands; mark `completed` in seed when the sprint closes or when a deliverable is truly done.

---

## Sprint theme

**Live Supabase desktop:** real auth, projects/contacts/jobs, Gmail Mailroom, operator profile, assistant on real data — while keeping **Mock mode** for demos. Shift product positioning toward **“operations assistant”** (OnPro AI + post-onboarding welcome) rather than menu-first SaaS.

---

## Summary (progress as of 2 June 2026)

Early June moved the app from **demo-only** toward **Live Supabase**: auth, core data loaders, Gmail Mailroom, Live/Mock toggle, profiles in Settings/sidebar, assistant API, and job costing / production depth.

**Latest (ledger sync — 2 Jun 2026, late):**

- **Ledger UI** — `/ledger` **Work completed by period**: expandable rows (click to show `detail` bullets); **month-only** labels (e.g. “June 2026”, not day dates). June invoice is **retainer drawdown only** — no per-task line items; whole OnPro job stays **$75k cap**. Retainers live in the Invoices table only.
- **Client signup & workspace linking** — migration `008_workspace_memberships.sql`: `workspace_memberships`, `pending_invites`, `workspace_member_events`, `contacts.linked_auth_user_id`; RPCs `find_workspaces_for_email`, `join_workspace`, `revoke_workspace_membership`, `resolve_invite_token`. Client vs operator **signup intent** on landing/login; client onboarding **step 2** workspace join cards; Live **invite create/resolve** + copy-link toast; owner **join notifications** + **revoke access** on contact detail. Run `007` + `008` in Supabase for Live.
- **OnPro AI briefing prefs** — migration `007_assistant_prefs.sql` (`profiles.assistant_prefs`); user can say “don’t show X on update” for any briefing section (projects, jobs, messages, contacts, Mailroom, calendar, documents). **Default brief** includes projects, jobs, and messages. Prefs merge session + DB on briefing/chat; removed OpenAI footer from assistant card.
- **Jobs hydrate fix** — project detail Jobs tab now uses `loadProjectJobs()` + `onpro-jobs-changed` (same path as Production board); jobs created on `/production` appear on project detail. Modal save to Supabase still open.
- **Contacts UI** — removed “People: …” line from contact detail header.
- **July planned** — `wr-ios-jul-backend` in seed (iOS Supabase parity; `status: pending`).

**Prior (2 Jun 2026, evening):**

- **Contacts directory** — nav/header/command palette **People → Contacts** (route stays `/people`); connect hero “Your production network in one place”.
- **CSV import** — `Import` on Contacts header; OpenAI + header fallback (`/api/contacts/import`); scrollable editable review; team/vendor/client + unlabeled rows; **auto-chunk** large files (200 rows/batch, user imports batch 1 → 2 → …); sample `public/samples/people-import-test.csv`; 2 MB upload cap.
- **Contact modals** — add client / team / vendor + pending invite detail use **section nav in left violet aside** (same pattern as job modal); removed bottom emoji illustrations on all polished modals.
- **Mailroom agent chat** — `/api/mailroom/chat` + `mailroom-chat-reply.ts`; conversational replies; **no task card** on questions (e.g. “any client tasks here?”); explicit asks only (`chat-intent.ts` fallback).
- **Messages** — Add Member on conversations (mock/LS threads; Live contacts for picker).
- **Ledger** — June deliverables are **12 `workRecord` rows** (no per-task pricing); expand each row under **Work completed by period** on `/ledger` for bullet detail. Retainers stay in the Invoices table only ($75k project cap).

**Prior (same sprint, already shipped):**

- Section connect covers; Live projects POST/PATCH; jobs UX (modal left nav, + New job, project picker, live cache); assistant full ops snapshot; workspace welcome; onboarding 4-step; Gmail Mailroom summarize/apply; costing/production depth.

**Still open in June:** job/WIP **save** to Supabase on modal save, Live Messages/calendar/documents **in DB**, messages/conversations RLS via workspace membership, invite deep links polish, iOS workspace-linking parity.

**July 2026 (planned):** iOS backend connection + app updates — `wr-ios-jul-backend` in seed (`status: pending`, linked to `inv-onpro-jul-2026`).

---

## Tasks — done or in progress (early June)

### Backend & data (Live vs Mock)

- [x] **Live / Mock toggle** beside OnPro logo — cookie `onpro_backend_mode`; clears live cache on switch.
- [x] **Live mode** — no mock fallbacks for projects, contacts, jobs; empty states for messages/calendar/documents until wired.
- [x] **Live data cache** — `live-cache.ts` + `LiveDataHydrator` for client-side Live hydration after server fetch.
- [x] **Mock stores gated** — `project-session`, `contacts-store`, `project-wip-edits` respect Live mode.
- [x] **BACKEND.md** — canonical pointers to `OnPro/SUPABASE_SCHEMA.sql`, iOS `SupabaseService`, integrations docs.
- [x] **Supabase auth (P0)** — SSR client, middleware, login page, `.env.example` for `NEXT_PUBLIC_SUPABASE_*`.
- [x] **Wire core data (P1–P2)** — `fetchProjects`, `fetchContacts`, `fetchJobs` via `src/lib/data/` + Supabase mappers.
- [x] **Job schema alignment** — `002_project_jobs.sql` / types from `wip.ts` for desktop job detail sections.

### Mailroom & Gmail

- [x] **Gmail OAuth** — Google Cloud client, test users, redirect `…/api/mailroom/gmail/callback`.
- [x] **Migration `003_user_gmail.sql`** — `user_gmail_connections` table (+ `update_updated_at`).
- [x] **Mailroom API routes** — connect, callback, threads, summarize, apply, status.
- [x] **Live Mailroom** — threads from connected Gmail only (not `MOCK_EMAIL_THREADS`).
- [x] **Removed promoted in-app threads** from Mailroom list (Gmail-only in Live).
- [x] **Email body pipeline** — `email-body.ts` (`htmlToPlainText`, `normalizeEmailBody`) for UI and OpenAI summarize.
- [x] **Mailroom agent inbox** — agent apply flow, mailroom state, promote-to-mailroom modal.
- [x] **Mailroom connect hero** — `mailroom-connect-hero.tsx` (LIVE · Gmail badge, 3-step flow, privacy note, secondary CTA).
- [x] **Conversational agent chat** — `/api/mailroom/chat`, OpenAI `mailroom-chat-reply.ts`, `lib/mailroom/chat-intent.ts` fallback; questions vs explicit draft requests.

### Assistant & Overview → OnPro AI

- [x] **Assistant API** — `/api/assistant/briefing`, `/api/assistant/chat` with OpenAI + Live ops snapshot.
- [x] **Live briefing** — no fictional overnight mock when Live; server fallback from real snapshot.
- [x] **Greeting uses signed-in user** — `ProfileProvider` + first name on Overview / assistant card.
- [x] **Sidebar “OnPro AI”** — first nav item (was Overview); command palette label updated.
- [x] **Workspace welcome assistant** — intent detection, structured reply links (`assistant-message-parts.tsx`, `workspace-welcome-reply.ts`).
- [x] **Full workspace snapshot** — `assistant-ops-snapshot.ts`: projects (rich fields), jobs (WIP steps), contacts (≤100), in-app messages + email threads with bodies, documents/calendar (mock), Gmail when connected, `coverage` JSON for the model.
- [x] **Briefing reply spacing** — `briefing-part-spacing.ts`; commas/status after project names; prompt + sanitize rules.
- [x] **Mock/live fallbacks** — contact/people questions in `mockAssistantReply` / `liveAssistantReply`; client fallback snapshot when API fails.
- [x] **Briefing section preferences** — migration `007_assistant_prefs.sql`; `profiles.assistant_prefs` JSONB; opt-out per section on “Update me” (projects, jobs, messages, contacts, Mailroom, calendar, documents); default brief includes projects + jobs + messages; merge session + DB prefs on briefing/chat; removed OpenAI footer from assistant card.

### Account & profile

- [x] **ProfileProvider** — loads `profiles` row for auth user; sidebar + Settings consume it.
- [x] **Sidebar footer** — logged-in name + company; Supabase avatar when set.
- [x] **Settings → Your account** — edit/save name, company, phone, business address/phone to Supabase `profiles`.
- [x] **Display name** — `profiles.username` (onboarding); signup trigger no longer pre-fills full name with email (`fullNameForOnboardingForm`).
- [x] **Avatar storage (Live)** — `005_storage_avatars.sql`, `upload-avatar.ts`, onboarding picker uploads to bucket `avatars`.
- [x] **next.config** — `images.remotePatterns` for Supabase storage host.
- [x] **Bugfix** — Settings “maximum update depth” loop.

### Jobs, costing & production

- [x] **Expanded job costing workflow** — costing sheet editor, templates, print helpers, estimates list.
- [x] **Vendor quotes, tech pack artwork** sections in job details.
- [x] **Job development / timeline templates** — `job-development.ts`, `job-timeline-templates.ts`, `job-number.ts`.
- [x] **Production board** — continued WIP/label/packing-slip integration with job modal.

### Projects UX

- [x] **New project modal** — split rocket design (`new-project-modal.tsx`).
- [x] **Add client in-modal** — horizontal slide panel; company / individual (`KindToggle`); saves via `commitSingleContact`; refreshes client dropdown.
- [x] **OPEN_NEW_PROJECT_EVENT** — workspace welcome “Start a project” opens modal on `/projects`.
- [x] **Persist projects to Supabase (Live)** — `/api/projects` POST/PATCH, `persist-project.ts`, list stays on `/projects` after create.
- [x] **Section covers** — `section-cover.ts`, `ops-section-cover.tsx`, per-section heroes; cover only when empty (unless `?cover=1`).

### Jobs UX (production)

- [x] **Jobs page header** — `+ New job` beside notifications (`CreateJobLauncher` on `production-page-content.tsx`).
- [x] **New job project picker** — `NewJobModal` + `CreateJobLauncher`; preset project from project detail; used in message composer too.
- [x] **Job details modal polish** — violet split layout; section nav in left aside; share menu only (no mock-mode banner).
- [x] **No demo jobs in Live** — `demo-seed-jobs.ts` filter on load/save/cache; placeholder copy fixes.
- [x] **Live jobs hydration** — `live-cache.ts` / `project-wip-edits.ts` / `production-page-content` job count + board refresh.
- [x] **Project detail jobs tab** — `project-jobs-view.tsx` uses `loadProjectJobs()` in Live (matches Production board); `onpro-jobs-changed` event on save.
- [ ] **Persist job saves to Supabase** on job modal save (localStorage + cache today).

### Workspace welcome (first visit)

- [x] **`WorkspaceWelcomeGate`** in `dashboard-shell.tsx` — portal modal after onboarding.
- [x] **Dismiss** — `profiles.workspace_welcome_dismissed_at` (migration `006`) + mock LS + session/`?welcome=1` fallback.
- [x] **UX** — X-only close (no backdrop dismiss); Gmail recommended; larger type; “OnPro AI tab” footer copy.
- [x] **Quick actions** — multi-intent filter from chat; links for Create project / Import contacts / etc.

### Onboarding (Live, DB-backed)

- [x] **Migration `004_profiles_onboarding.sql`** — onboarding columns on `profiles`.
- [x] **Operator flow** — **4 steps** (workspace → business → team → launch); public landing when signed out; post-signup straight to wizard.
- [x] **Client flow** — 2-step signup; redirect to messages (`/login?kind=client&next=…`); **step 2 workspace join** when email matches existing operator workspace.
- [x] **Signup intent** — client vs operator choice on landing/login before account creation (`signup-intent-landing.tsx`).
- [x] **API routes** — `/api/onboarding/status`, `/operator`, `/client`, `/init`; DB-only (`OnboardingSchemaError` if 004 missing).
- [x] **Middleware gate** — incomplete → `/onboarding`; completed → workspace with `?welcome=1`.
- [x] **Self on Team** — `ensureSelfTeamContact` + `profiles.self_contact_id`.
- [x] **Onboarding UI** — avatar under header, launch “What’s next”, client 2-step celebration grid.

### Contacts directory (Live CRUD + import)

- [x] **Contacts API** — `POST /api/contacts` (upsert contact, update permissions).
- [x] **Live persistence** — `persist-contact.ts`, `commit-contacts.ts`, `contacts-write.ts`, `contact-payload.ts`.
- [x] **Rename People → Contacts** — sidebar, page header, command palette “Go to”.
- [x] **Add modals** — client / teammate / vendor; section nav in **left violet aside**; company + individual clients.
- [x] **Pending invite detail modal** — Overview / Permissions / After accept; polished shell.
- [x] **CSV import** — `import-clients-modal.tsx`, `/api/contacts/import`, AI `parse-contacts-csv.ts`, fallback parser, review + validation, **auto-chunk** (`split-csv-import-chunks.ts`, 200/batch).
- [x] **Connect hero** — “Your production network in one place”.
- [x] **Project roster dedupe** — `dedupe-projects.ts` fixes duplicate `<option>` keys.
- [x] **Detail sheet** — permission edits persist via API in Live.
- [x] **Contact detail header** — removed redundant “People: …” line from detail modal.
- [x] **Pending invites (Live)** — `008` + `/api/invites/*`; token resolve on login; copy-link toast; pending invite detail modal reads Supabase in Live.
- [x] **Workspace membership** — client self-signup email match + confirm-to-join; owner revoke on contact detail; join events in notifications bell.
- [ ] **Invite deep links** — polish client invite → onboarding/messages flow.
- [ ] **Messages RLS** — conversations readable via workspace membership (not wired yet).

### Messages

- [x] **Add Member** — conversation picker, deduped members (`add-conversation-member-modal.tsx`).
- [ ] **Live Supabase messaging** — per `docs/MESSAGING_DECISION.md` (UI still mock/localStorage).

### UI polish

- [x] **Sidebar** — removed decorative alert dots on nav items.
- [x] **Notifications** — bell badge only when real notification items exist.
- [x] **Toasts** — viewport at top of screen.
- [x] **Modal aside** — removed bottom emoji illustrations (`ProjectModalIllustration`); cleaner purple column on import, contacts, jobs, mailroom, calendar, documents, etc.

### Workspace linking & invites (Live)

- [x] **Migration `008_workspace_memberships.sql`** — `workspace_memberships`, `pending_invites`, `workspace_member_events`, `contacts.linked_auth_user_id`; RLS for member project read; SECURITY DEFINER RPCs.
- [x] **Email match API** — `GET /api/onboarding/workspace-matches`, `POST /api/onboarding/join-workspace`.
- [x] **Invite APIs** — `POST /api/invites/create`, `GET /api/invites`, `GET /api/invites/resolve`; token login links from add-contact modals.
- [x] **Membership admin** — `GET /api/workspace/memberships`, `POST /api/workspace/memberships/revoke`, `GET /api/workspace/member-events` (owner notifications).
- [x] **Client onboarding** — workspace cards on step 2; `saveClientProfileFields` + join flow in `/api/onboarding/client`.
- [ ] **iOS parity** — workspace linking on native app (Phase 3).

### Ledger UI (`/ledger`)

- [x] **Work completed by period** — expandable rows; month-only `periodLabel` (no day dates in table).
- [x] **June invoice** — retainer drawdown only on `inv-onpro-jun-2026`; no per-task line items ($75k project cap unchanged).
- [x] **Deliverable detail** — each June `workRecord` has `description` + `detail` bullets visible on expand.
- [x] **July line** — `wr-ios-jul-backend` pending, linked to `inv-onpro-jul-2026`.

### Planning & docs

- [x] **Messaging decision doc** — `docs/MESSAGING_DECISION.md`.
- [x] **Supabase setup runbook** — dashboard SQL for schema + Gmail migration.
- [x] **Storage avatars runbook** — `docs/supabase-storage-avatars.md`.
- [x] **Development ledger** — this file + `seed.ts` sync (June work lines).

---

## Tasks — planned for rest of June (sprint backlog)

- [ ] **Persist job/WIP edits to Supabase** on save (production board, project detail, job modal).
- [ ] **Live Messages** — wire `conversations` / chosen messaging stack (per `MESSAGING_DECISION.md`).
- [ ] **Live Calendar & Documents** — read path from Supabase or defer with clear empty states.
- [ ] **Mailroom hardening** — error states, token refresh, rate limits; agent apply audit trail.
- [ ] **Projects delete** — Live delete already via API; polish/error UX as needed.
- [ ] **Messages RLS via membership** — workspace members read shared conversations (008 schema ready).
- [ ] **Assistant** — OpenAI guardrails, 429 handling, optional caching of daily briefing.
- [ ] **Dedicated `/welcome` test route** (optional; today use `/projects?welcome=1`).
- [ ] **iOS parity** — workspace linking, profile + Mailroom aligned with `SupabaseService`.
- [ ] **Ledger close-out** — mark June `workRecords` completed in seed when sprint closes.

---

## `workRecords` in `seed.ts` (synced)

June retainer invoice: **`inv-onpro-jun-2026`** ($1,500). In [`src/lib/ledger/seed.ts`](../../src/lib/ledger/seed.ts):

- Invoice row is the **retainer drawdown only** — no per-task line items (whole OnPro job is **$75k cap**).
- Each June `workRecord` has **`description`** (table title) + **`detail`** (bullet breakdown) — visible on `/ledger` → **Work completed by period** (click row to expand).
- **`periodLabel`** uses month names only (e.g. “June 2026”), not day-level dates.

| ID | Description (short) | Detail covers |
|----|---------------------|---------------|
| `wr-next-jun-live` | Live Supabase path + data loaders | Toggle, auth, loaders, BACKEND.md, migrations 002 |
| `wr-next-jun-mailroom` | Gmail Mailroom + agent inbox + chat | OAuth, APIs, summarize/apply, **conversational chat**, connect hero |
| `wr-next-jun-onboarding` | Onboarding + profiles + avatars | 4-step onboarding, avatars, workspace welcome SQL, **client signup intent** |
| `wr-next-jun-contacts` | Contacts directory, import, modals | Live contacts API, **CSV import + chunking**, modals, **Live invites** |
| `wr-next-jun-welcome-ai` | Workspace welcome + OnPro AI | Welcome gate, assistant API, nav rename, **briefing section prefs (007)** |
| `wr-next-jun-projects-ux` | Projects UX + Live create/edit API | New project, add client slide-in, POST/PATCH projects |
| `wr-next-jun-covers` | Section connect covers | All ops heroes, ℹ️ / `?cover=1`, Contacts cover |
| `wr-next-jun-jobs-ux` | Jobs UX — create flow + modal polish | + New job, project picker, left nav modal, **project detail hydrate fix** |
| `wr-next-jun-jobs` | Costing, estimates, production depth | Costing sheet, estimates, vendor quotes, board |
| `wr-next-jun-assistant-profile` | Assistant API + full ops snapshot | Full ops snapshot, Settings account, avatars |
| `wr-next-jun-workspace-link` | Client signup + workspace linking | **008** memberships, email match join, invite tokens, revoke, notifications |
| `wr-next-jun-ui-docs` | Runbooks, messaging decision, UI polish | Messages Add Member, modal polish, MESSAGING_DECISION, **ledger UI** |

**Cap systems:** `cap-next` **94%** (workspace UI); `cap-supabase-api` **~78%** (Live backend — see breakdown below).

### Backend cap estimate (`cap-supabase-api` @ ~78%)

Based on what **works in Live mode** today (not Mock/localStorage):

| Area | Est. | Notes |
|------|------|--------|
| Auth + Supabase clients + migrations | ~100% | Login, middleware, `002`–`008` migrations |
| Projects API + Supabase | ~95% | `POST`/`PATCH` `/api/projects`, list/detail Live |
| Contacts / People API | ~90% | `/api/contacts`, permissions persist |
| Profiles, onboarding, avatars | ~95% | Onboarding routes, `profiles`, storage bucket |
| Workspace memberships + invites | ~85% | **008** — join, revoke, pending invites, member events |
| Assistant prefs + briefing | ~92% | **007** `assistant_prefs`; section opt-out on Update me |
| Mailroom + Gmail (Node APIs) | ~88% | OAuth, threads, summarize, apply, status, chat |
| Assistant (Node APIs) | ~90% | `/api/assistant/briefing`, `/api/assistant/chat` |
| Jobs data plane | ~42% | **Read** + client hydrate; modal **save still localStorage** |
| Messages | ~5% | UI only; no Live DB |
| Documents / Calendar | 0% | Live pages return **empty** (covers only) |

**Still pulls 0% down:** in-app messaging DB, documents/calendar tables, job WIP write path, messages RLS via membership.

---

## Out of scope for June sprint (ledger unchanged)

- Supabase / Next.js API completion & admin platform  
- DROPX / FBRC deliverables (separate ledger lines)  
- Android / advanced analytics (future expansion TBD)

---

## Key files (reference)

| Area | Paths |
|------|--------|
| Live/Mock | `src/lib/config/backend-mode.ts`, `src/components/backend-mode-toggle.tsx` |
| Profile | `src/components/profile-provider.tsx`, `src/lib/supabase/profile.ts`, `src/lib/supabase/upload-avatar.ts` |
| Onboarding | `src/app/onboarding/**`, `src/components/onboarding/*`, `supabase/migrations/004_profiles_onboarding.sql` |
| Workspace welcome | `src/components/workspace-welcome-modal.tsx`, `workspace-welcome-gate.tsx`, `src/lib/workspace-welcome-*.ts`, `supabase/migrations/006_workspace_welcome.sql` |
| People / contacts | `src/components/people-view.tsx`, `src/components/add-contact-modals.tsx`, `src/app/api/contacts/route.ts` |
| Projects | `src/components/new-project-modal.tsx`, `projects-page-content.tsx`, `src/app/api/projects/route.ts`, `src/lib/data/persist-project.ts` |
| Section covers | `src/lib/section-cover.ts`, `src/components/ops-section-cover.tsx`, `*-connect-hero.tsx`, `*-page-content.tsx` |
| Jobs | `src/components/job-details-modal.tsx`, `create-job-launcher.tsx`, `new-job-modal.tsx`, `production-page-content.tsx`, `src/lib/mock/demo-seed-jobs.ts` |
| Mailroom | `src/components/mailroom-view.tsx`, `src/app/api/mailroom/**`, `src/lib/openai/mailroom-chat-reply.ts`, `src/lib/mailroom/chat-intent.ts` |
| Contacts import | `src/components/import-clients-modal.tsx`, `src/app/api/contacts/import/route.ts`, `src/lib/csv/*`, `public/samples/people-import-test.csv` |
| Assistant | `src/app/api/assistant/**`, `src/lib/server/assistant-ops-snapshot.ts`, `src/lib/assistant/prefs.ts`, `src/lib/supabase/assistant-prefs.ts`, `overview-assistant.tsx` |
| Workspace linking | `supabase/migrations/008_workspace_memberships.sql`, `src/lib/supabase/workspace-memberships.ts`, `src/app/api/invites/**`, `src/app/api/onboarding/workspace-matches`, `src/app/api/onboarding/join-workspace`, `src/app/api/workspace/**` |
| Notifications | `src/components/notifications-popover.tsx`, `src/lib/notifications.ts` |
| Nav | `src/components/app-sidebar.tsx`, `src/components/command-palette.tsx` |
| Config | `next.config.ts` (Supabase image hosts) |
| Ledger UI + seed | `src/app/ledger/**`, `src/lib/ledger/seed.ts` |

---

## Supabase migrations (Live checklist)

Run in SQL Editor (paste file contents, in order):

1. `OnPro/SUPABASE_SCHEMA.sql` (base tables)
2. `supabase/migrations/003_user_gmail.sql`
3. `supabase/migrations/004_profiles_onboarding.sql`
4. `supabase/migrations/005_storage_avatars.sql`
5. `supabase/migrations/006_workspace_welcome.sql`
6. `supabase/migrations/007_assistant_prefs.sql`
7. `supabase/migrations/008_workspace_memberships.sql`

Then: `NOTIFY pgrst, 'reload schema';`

---

*Sprint doc started: 1 June 2026 — last updated: 2 June 2026 (workspace linking, assistant briefing prefs, jobs hydrate fix, ledger UI expandable rows + 12 deliverables).*
